#ifndef FUNCTIONS_HEADER
#define FUNCTIONS_HEADER

#include <stdio.h>
#include <string.h>

int palindrome(char*);
int fibonacci(unsigned int);
void swap(int*, int*);
int minimum(int[], int);
int maximum(int[], int);

#endif
